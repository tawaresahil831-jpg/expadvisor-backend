---
phase: 02-automated-testing-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/requirements.txt
  - backend/app/config.py
  - backend/app/__init__.py
  - backend/pytest.ini
  - backend/tests/conftest.py
  - backend/tests/test_auth.py
  - backend/tests/test_experiences.py
  - backend/tests/test_comments_likes.py
autonomous: true
requirements:
  - TEST-01
user_setup: []
must_haves:
  truths:
    - "backend/app/__init__.py allows passing configuration overrides into create_app"
    - "Pytest runs in-memory without connecting to live remote PostgreSQL"
    - "Test suites for auth, experiences, comments, and likes pass with 100% green status"
  artifacts:
    - backend/pytest.ini
    - backend/tests/conftest.py
    - backend/tests/test_auth.py
    - backend/tests/test_experiences.py
    - backend/tests/test_comments_likes.py
  key_links:
    - "conftest.py sets up app with TestConfig, creates tables in SQLite memory, and yields test client"
---

<objective>
Implement isolated, fast automated unit and integration tests using Pytest with in-memory SQLite database fixtures.

Purpose: Provide reproducible local test verification for backend endpoints and business logic without external network/database dependencies.
Output: Pytest configuration, fixtures, and complete test suites for Auth, Experiences, Comments, and Likes.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-automated-testing-infrastructure/02-RESEARCH.md
@.planning/phases/02-automated-testing-infrastructure/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install pytest and update create_app to accept custom config</name>
  <files>backend/requirements.txt, backend/app/config.py, backend/app/__init__.py</files>
  <read_first>backend/app/config.py, backend/app/__init__.py</read_first>
  <action>
    1. Add `pytest>=8.0.0` to `backend/requirements.txt` and install it in `backend/venv`.
    2. Add `TestConfig` in `backend/app/config.py`:
       ```python
       class TestConfig(Config):
           TESTING = True
           SQLALCHEMY_DATABASE_URI = "sqlite:///:memory:"
           RATELIMIT_ENABLED = False
           SECRET_KEY = "test-secret-key"
           JWT_SECRET_KEY = "test-jwt-secret-key"
       ```
    3. Update `create_app(config_class=Config)` in `backend/app/__init__.py` to accept `config_class` as an argument.
  </action>
  <verify>backend/venv/bin/python -c "from app.config import TestConfig; from app import create_app; app = create_app(TestConfig); assert app.config['TESTING']; print('TestConfig OK')"</verify>
  <acceptance_criteria>
    - pytest installed in virtual environment
    - TestConfig defined in backend/app/config.py
    - create_app(TestConfig) initializes cleanly with SQLite in-memory database
  </acceptance_criteria>
  <done>Flask app factory supports injecting TestConfig</done>
</task>

<task type="auto">
  <name>Task 2: Setup conftest.py and test fixtures</name>
  <files>backend/pytest.ini, backend/tests/conftest.py</files>
  <read_first>backend/app/extensions.py</read_first>
  <action>
    1. Create `backend/pytest.ini` with testpaths and pythonpath set to `.`.
    2. Create `backend/tests/conftest.py` providing:
       - `app` fixture (creates tables on setup, drops tables on teardown)
       - `client` fixture (`app.test_client()`)
       - `auth_headers` fixture (registers a verified user, generates JWT token, returns Authorization headers)
  </action>
  <verify>backend/venv/bin/pytest --version</verify>
  <acceptance_criteria>
    - backend/pytest.ini exists
    - backend/tests/conftest.py exists and configures fixtures
  </acceptance_criteria>
  <done>Pytest test harness and fixtures configured</done>
</task>

<task type="auto">
  <name>Task 3: Author test suites for Auth, Experiences, Comments, and Likes</name>
  <files>backend/tests/test_auth.py, backend/tests/test_experiences.py, backend/tests/test_comments_likes.py</files>
  <read_first>backend/app/routes/auth.py, backend/app/routes/experience.py, backend/app/routes/comment.py</read_first>
  <action>
    1. Create `test_auth.py`:
       - User registration (valid payload vs missing fields)
       - Duplicate email registration rejection
       - OTP verification and login
       - Current user fetch (`/api/auth/me`)
    2. Create `test_experiences.py`:
       - Experience creation with auth
       - Single experience retrieval and view counter
       - Pagination and category filtering
       - Unauthorized update/delete prevention
       - Authorized update and deletion
    3. Create `test_comments_likes.py`:
       - Adding comment to experience
       - Marking comment as accepted answer
       - Upvoting/liking experience and verifying count increment/decrement
    4. Run full pytest suite and ensure all tests pass.
  </action>
  <verify>backend/venv/bin/pytest backend/tests -v</verify>
  <acceptance_criteria>
    - All test files in backend/tests execute cleanly
    - 0 test failures, 0 errors
  </acceptance_criteria>
  <done>Full automated test suite implemented and passing</done>
</task>

</tasks>
