---
phase: 02-automated-testing-infrastructure
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - TEST-02
user_setup: []
must_haves:
  truths:
    - ".github/workflows/ci.yml defines continuous integration on push and pull_request"
    - "CI workflow sets up Python, installs backend requirements and pytest, and runs test suite"
  artifacts:
    - .github/workflows/ci.yml
  key_links:
    - "CI workflow triggers pytest backend/tests"
---

<objective>
Configure GitHub Actions Continuous Integration (CI) to automatically test all pull requests and pushes to the main branch.

Purpose: Prevent regressions and maintain code quality automatically on every code change.
Output: GitHub Actions CI workflow file at `.github/workflows/ci.yml`.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-automated-testing-infrastructure/02-RESEARCH.md
@.planning/phases/02-automated-testing-infrastructure/02-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GitHub Actions CI workflow</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>backend/requirements.txt</read_first>
  <action>
    1. Create directory `.github/workflows/`.
    2. Write `.github/workflows/ci.yml`:
       - Trigger on `push` to `main` and `pull_request` to `main`.
       - Job `test` on `ubuntu-latest`.
       - Steps:
         - Checkout code (`actions/checkout@v4`)
         - Setup Python 3.10 (`actions/setup-python@v5`)
         - Cache pip dependencies
         - Install dependencies from `backend/requirements.txt`
         - Run `pytest backend/tests`
  </action>
  <verify>test -f .github/workflows/ci.yml && git status -s .github/workflows/ci.yml</verify>
  <acceptance_criteria>
    - .github/workflows/ci.yml exists
    - YAML syntax is valid
    - Targets Python 3.10/3.11 with pytest backend/tests
  </acceptance_criteria>
  <done>Continuous Integration pipeline configured for GitHub repository</done>
</task>

</tasks>
