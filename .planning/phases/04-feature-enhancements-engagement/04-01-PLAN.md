---
phase: 04-feature-enhancements-engagement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/routes/notification.py
  - backend/tests/test_notifications_bookmarks.py
  - expadvisor-frontend/frontend/js/api.js
autonomous: true
requirements:
  - ENG-01
user_setup: []
must_haves:
  truths:
    - "Backend provides unread count or unread filter for notifications"
    - "Frontend client dynamically loads notification badge count and handles mark-as-read"
    - "Automated unit tests verify notification retrieval and mark-read workflows"
  artifacts:
    - backend/tests/test_notifications_bookmarks.py
  key_links:
    - "api.js fetchNotifications updates UI badge counter element in the header"
---

<objective>
Enhance the notification system with unread counts, mark-as-read endpoints, automated test coverage, and live UI badge counter integration in the frontend header.

Purpose: Increase user engagement by providing clear alerts when comments or likes are received on experiences.
Output: Enhanced notification routes, automated unit tests, and client badge handler.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-feature-enhancements-engagement/04-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add unread count endpoint and automated tests for notifications</name>
  <files>backend/app/routes/notification.py, backend/tests/test_notifications_bookmarks.py</files>
  <read_first>backend/app/routes/notification.py</read_first>
  <action>
    1. In `backend/app/routes/notification.py`, add `GET /api/notifications/unread-count`:
       - Query count of `Notification` where `user_id == current_user.user_id` and `is_read == False`.
       - Return JSON envelope: `{"success": True, "data": {"unread_count": count}}`.
    2. Create `backend/tests/test_notifications_bookmarks.py`:
       - Test fetching notifications
       - Test unread count endpoint
       - Test marking single notification as read
       - Test marking all as read
  </action>
  <verify>backend/venv/bin/pytest backend/tests/test_notifications_bookmarks.py -v</verify>
  <acceptance_criteria>
    - GET /api/notifications/unread-count returns 200 OK with unread_count
    - All notification tests pass with 0 failures
  </acceptance_criteria>
  <done>Notification routes enhanced and verified via pytest</done>
</task>

<task type="auto">
  <name>Task 2: Integrate live notification badge in client frontend</name>
  <files>expadvisor-frontend/frontend/js/api.js</files>
  <read_first>expadvisor-frontend/frontend/js/api.js</read_first>
  <action>
    1. In `expadvisor-frontend/frontend/js/api.js`, implement `updateNotificationBadge()`:
       - Calls `/notifications/unread-count`.
       - If `unread_count > 0`, updates `#notificationBadge` or `.notification-badge` elements with count and removes `hidden`.
       - If 0, hides the badge element.
    2. Add call to `updateNotificationBadge()` inside `populateUserProfile()` and whenever notifications are fetched or marked read.
  </action>
  <verify>grep -rn "updateNotificationBadge" expadvisor-frontend/frontend/js/api.js</verify>
  <acceptance_criteria>
    - api.js contains updateNotificationBadge
    - Invoked automatically for authenticated users
  </acceptance_criteria>
  <done>Header notification badge dynamically reflects unread notifications</done>
</task>

</tasks>
