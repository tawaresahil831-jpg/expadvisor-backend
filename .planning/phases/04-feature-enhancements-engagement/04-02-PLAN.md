---
phase: 04-feature-enhancements-engagement
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - backend/app/models/__init__.py
  - backend/app/models/bookmark.py
  - backend/app/routes/bookmark.py
  - backend/app/__init__.py
  - backend/tests/test_notifications_bookmarks.py
  - expadvisor-frontend/frontend/js/api.js
autonomous: true
requirements:
  - ENG-02
  - ENG-03
user_setup: []
must_haves:
  truths:
    - "Users can save/bookmark experiences and view their saved list"
    - "Backend provides bookmark toggle and listing endpoints"
    - "Automated unit tests verify bookmark creation, idempotency, and removal"
  artifacts:
    - backend/app/models/bookmark.py
    - backend/app/routes/bookmark.py
  key_links:
    - "backend/app/__init__.py registers bookmark_bp at /api/bookmarks"
---

<objective>
Implement the Bookmark / Saved Experiences feature across backend models, routes, test suites, and client-side API actions.

Purpose: Allow students to save valuable interview and internship experiences to revisit later.
Output: `Bookmark` model, `/api/bookmarks` endpoints, test coverage, and client integration.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-feature-enhancements-engagement/04-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Bookmark model and API routes</name>
  <files>
    backend/app/models/bookmark.py,
    backend/app/models/__init__.py,
    backend/app/routes/bookmark.py,
    backend/app/__init__.py
  </files>
  <read_first>backend/app/models/like.py</read_first>
  <action>
    1. Create `backend/app/models/bookmark.py`:
       - Table `bookmarks` with `id`, `user_id`, `experience_id`, `created_at`.
       - Unique constraint on `(user_id, experience_id)`.
       - Relationships to `User` and `Experience`.
       - `to_dict()` serialization.
    2. Export `Bookmark` from `backend/app/models/__init__.py`.
    3. Create `backend/app/routes/bookmark.py`:
       - `POST /api/experiences/<id>/bookmark`: Toggle bookmark state (adds if not exists, removes if exists). Returns `{"is_bookmarked": bool}`.
       - `GET /api/bookmarks`: Returns paginated list of bookmarked experiences for `current_user`.
    4. Register `bookmark_bp` in `backend/app/__init__.py`.
  </action>
  <verify>backend/venv/bin/python -c "from app.models import Bookmark; from app.routes.bookmark import bookmark_bp; print('Bookmark imports OK')"</verify>
  <acceptance_criteria>
    - Bookmark model defined with unique constraint
    - bookmark_bp registered in create_app
  </acceptance_criteria>
  <done>Bookmark backend domain logic implemented</done>
</task>

<task type="auto">
  <name>Task 2: Add test coverage and client integration for bookmarks</name>
  <files>
    backend/tests/test_notifications_bookmarks.py,
    expadvisor-frontend/frontend/js/api.js
  </files>
  <read_first>backend/tests/test_notifications_bookmarks.py</read_first>
  <action>
    1. Add tests in `backend/tests/test_notifications_bookmarks.py`:
       - Test toggling bookmark on and off
       - Test listing user bookmarks
       - Test unauthenticated bookmark access blocked (401)
    2. In `expadvisor-frontend/frontend/js/api.js`:
       - Add `toggleBookmark(experienceId)` helper function.
       - Add `fetchBookmarks()` helper function.
    3. Run full pytest suite to verify all backend tests pass.
  </action>
  <verify>backend/venv/bin/pytest backend/tests -v</verify>
  <acceptance_criteria>
    - All pytest suites pass cleanly
    - api.js exposes toggleBookmark and fetchBookmarks
  </acceptance_criteria>
  <done>Bookmarks fully tested and client-enabled</done>
</task>

</tasks>
