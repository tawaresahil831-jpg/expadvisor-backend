---
phase: 01-codebase-hygiene-repository-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/run.py
  - backend/requirements.txt
  - backend/Procfile
  - backend/app/__init__.py
  - backend/app/models/__init__.py
  - backend/app/models/user.py
  - backend/app/models/experience.py
  - backend/app/models/comment.py
  - backend/app/models/like.py
  - backend/app/models/notification.py
  - backend/app/routes/like.py
  - backend/app/routes/notification.py
  - backend/app/routes/user.py
  - backend/app/utils/email_utils.py
autonomous: true
requirements:
  - HYG-01
user_setup: []
must_haves:
  truths:
    - "backend/ directory contains all models, routes, run.py, and requirements.txt to run independently"
    - "No active source files contain hardcoded absolute references to /Users/sahiltaware415/expadvisor-backend"
    - "Flask application starts and imports successfully from within backend/"
  artifacts:
    - backend/run.py
    - backend/requirements.txt
    - backend/app/models/user.py
    - backend/app/models/experience.py
    - backend/app/models/comment.py
    - backend/app/models/like.py
    - backend/app/models/notification.py
    - backend/app/routes/like.py
    - backend/app/routes/notification.py
    - backend/app/routes/user.py
  key_links:
    - "backend/run.py imports create_app from app and starts the WSGI server"
    - "backend/app/__init__.py registers blueprints for auth, experience, comment, like, upload, notification, user, and admin"
---

<objective>
Consolidate backend source files directly into the workspace `backend/` directory and eliminate all external filesystem dependencies on `/Users/sahiltaware415/expadvisor-backend`.

Purpose: Make ExpAdvisor's backend completely self-contained and reproducible within this repository.
Output: Fully populated `backend/` directory with models, routes, utils, configuration, and runner scripts.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/STACK.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/phases/01-codebase-hygiene-repository-consolidation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Synchronize missing models, routes, and runner from expadvisor-backend</name>
  <files>
    backend/run.py,
    backend/requirements.txt,
    backend/Procfile,
    backend/app/models/,
    backend/app/routes/like.py,
    backend/app/routes/notification.py,
    backend/app/routes/user.py,
    backend/app/utils/email_utils.py
  </files>
  <read_first>/Users/sahiltaware415/expadvisor-backend/run.py, /Users/sahiltaware415/expadvisor-backend/requirements.txt</read_first>
  <action>
    1. Copy missing models directory (`/Users/sahiltaware415/expadvisor-backend/app/models/`) into `backend/app/models/`.
    2. Copy missing routes (`like.py`, `notification.py`, `user.py`) from `/Users/sahiltaware415/expadvisor-backend/app/routes/` into `backend/app/routes/`.
    3. Copy missing utils (`email_utils.py`) from `/Users/sahiltaware415/expadvisor-backend/app/utils/` into `backend/app/utils/`.
    4. Copy root runner files (`run.py`, `requirements.txt`, `Procfile`) from `/Users/sahiltaware415/expadvisor-backend/` into `backend/`.
    5. Ensure `backend/app/__init__.py` properly registers `user_bp` and `notification_bp` alongside existing blueprints.
  </action>
  <verify>test -f backend/run.py && test -d backend/app/models && test -f backend/app/routes/like.py && echo "BACKEND_FILES_OK"</verify>
  <acceptance_criteria>
    - backend/run.py exists
    - backend/requirements.txt exists
    - backend/app/models/__init__.py exists and contains User, Experience, Comment, Like, Notification
    - backend/app/routes/like.py exists
    - backend/app/routes/notification.py exists
  </acceptance_criteria>
  <done>All models, routes, and runner files present in backend/ directory</done>
</task>

<task type="auto">
  <name>Task 2: Eliminate external hardcoded directory references</name>
  <files>test_regression.py, expadvisor-frontend/frontend/js/api.js</files>
  <read_first>test_regression.py</read_first>
  <action>
    1. Audit and verify that active application code and tests do not refer to `/Users/sahiltaware415/expadvisor-backend`.
    2. Update any relative paths or environment configurations so tests and runners target the local `backend/` or configurable `BASE_URL`.
  </action>
  <verify>grep -rn "expadvisor-backend" backend/ expadvisor-frontend/frontend/ 2>/dev/null || echo "CLEAN"</verify>
  <acceptance_criteria>
    - grep for "expadvisor-backend" inside backend/ returns 0 matches
    - grep for "expadvisor-backend" inside expadvisor-frontend/frontend/ returns 0 matches
  </acceptance_criteria>
  <done>Zero references to external expadvisor-backend in active backend and frontend code</done>
</task>

<task type="auto">
  <name>Task 3: Validate standalone backend importability</name>
  <files>backend/app/__init__.py</files>
  <read_first>backend/run.py</read_first>
  <action>
    Run a Python sanity check inside `backend/` to verify that `from app import create_app` loads all models, extensions, and route blueprints without missing import errors.
  </action>
  <verify>python3 -c "import sys; sys.path.insert(0, 'backend'); from app import create_app; app = create_app(); print('App initialized successfully')"</verify>
  <acceptance_criteria>
    - python3 import check exits with code 0
    - Output displays "App initialized successfully"
  </acceptance_criteria>
  <done>Flask backend initializes cleanly from within the workspace without external directory dependencies</done>
</task>

</tasks>
