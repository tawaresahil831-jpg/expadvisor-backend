---
phase: 01-codebase-hygiene-repository-consolidation
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - .gitignore
  - backend/.gitignore
  - expadvisor-frontend/.gitignore
files_deleted:
  - patch_db_views.py
  - patch_auth_google.py
  - patch_experience_routes.py
  - patch_experience_model.py
  - expadvisor-frontend/fix_syntax.py
  - expadvisor-frontend/fix_register.py
  - expadvisor-frontend/patch_login.py
  - expadvisor-frontend/patch_pagination_frontend.py
  - expadvisor-frontend/fix_fields.py
  - expadvisor-frontend/check_db.py
  - expadvisor-frontend/patch_fix_dummy.py
  - expadvisor-frontend/clean_dummy_articles.py
  - expadvisor-frontend/patch_remove_dummy.py
  - expadvisor-frontend/test_api.py
  - expadvisor-frontend/patch_frontend_google.py
  - expadvisor-frontend/del_test.py
  - expadvisor-frontend/fix_checkbox.py
  - expadvisor-frontend/patch_accepted_answer.py
  - expadvisor-frontend/patch_hashtag.py
autonomous: true
requirements:
  - HYG-02
  - HYG-03
user_setup: []
must_haves:
  truths:
    - "Obsolete root and frontend patch scripts are safely removed or archived"
    - "Unified root git repository tracks the entire monorepo with appropriate .gitignore rules"
    - "expadvisor-frontend-test backup folder is cleanly archived or removed to prevent redundancy"
  artifacts:
    - .gitignore
  key_links:
    - "Root .gitignore excludes sensitive files (.env, virtualenvs, __pycache__, logs)"
---

<objective>
Safely purge obsolete single-use patch scripts, clean up orphaned test artifacts, and establish a unified Git repository covering both backend and frontend.

Purpose: Clear technical debt and clutter, preventing confusion between active code and legacy hotfixes.
Output: Clean root directory, proper root `.gitignore`, and unified Git tracking.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-codebase-hygiene-repository-consolidation/01-RESEARCH.md
@.planning/phases/01-codebase-hygiene-repository-consolidation/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Archive and purge obsolete ad-hoc patch scripts</name>
  <files>
    patch_db_views.py,
    patch_auth_google.py,
    patch_experience_routes.py,
    patch_experience_model.py,
    expadvisor-frontend/fix_syntax.py,
    expadvisor-frontend/fix_register.py,
    expadvisor-frontend/patch_login.py,
    expadvisor-frontend/patch_pagination_frontend.py,
    expadvisor-frontend/fix_fields.py,
    expadvisor-frontend/check_db.py,
    expadvisor-frontend/patch_fix_dummy.py,
    expadvisor-frontend/clean_dummy_articles.py,
    expadvisor-frontend/patch_remove_dummy.py,
    expadvisor-frontend/test_api.py,
    expadvisor-frontend/patch_frontend_google.py,
    expadvisor-frontend/del_test.py,
    expadvisor-frontend/fix_checkbox.py,
    expadvisor-frontend/patch_accepted_answer.py,
    expadvisor-frontend/patch_hashtag.py,
    expadvisor-frontend/frontend/temp_script*.js
  </files>
  <read_first>expadvisor-frontend/check_db.py</read_first>
  <action>
    1. Create an archive tarball or directory if needed, or remove dead single-use scripts that are no longer referenced in production.
    2. Remove obsolete patch scripts (`patch_*.py`, `fix_*.py`, `clean_*.py`, `del_*.py`, `test_api.py`, `check_db.py`) from workspace root and `expadvisor-frontend/`.
    3. Remove transient `temp_script*.js` files inside `expadvisor-frontend/frontend/`.
    4. Remove obsolete `expadvisor-frontend/Backend/` empty/stub folder.
  </action>
  <verify>ls patch_*.py 2>/dev/null || echo "ROOT_PATCHES_CLEAN"</verify>
  <acceptance_criteria>
    - No patch_*.py files exist at root
    - No fix_*.py files exist in expadvisor-frontend
    - No temp_script*.js files exist in expadvisor-frontend/frontend
  </acceptance_criteria>
  <done>All legacy single-use patch scripts removed from active directories</done>
</task>

<task type="auto">
  <name>Task 2: Clean up redundant test mirror directory</name>
  <files>expadvisor-frontend-test/</files>
  <read_first>expadvisor-frontend-test/frontend/index.html</read_first>
  <action>
    Clean up or remove the redundant test copy directory `expadvisor-frontend-test` which is a duplicate of `expadvisor-frontend`.
  </action>
  <verify>test ! -d expadvisor-frontend-test && echo "TEST_MIRROR_REMOVED"</verify>
  <acceptance_criteria>
    - expadvisor-frontend-test directory no longer clutters root workspace
  </acceptance_criteria>
  <done>Redundant frontend test clone removed</done>
</task>

<task type="auto">
  <name>Task 3: Establish root .gitignore and unified Git tracking</name>
  <files>.gitignore</files>
  <read_first>expadvisor-frontend/.gitignore</read_first>
  <action>
    1. Create a root `.gitignore` specifying:
       - `.env` and `*.env.local`
       - `venv/` and `env/`
       - `__pycache__/` and `*.pyc`
       - `.DS_Store`
       - `.vercel`
       - `instance/*.db`
    2. Initialize a unified Git repository at workspace root (`git init`).
    3. Remove nested `.git` folder from `expadvisor-frontend/` so git treats the entire repository as a cohesive monorepo rather than an unmanaged submodule.
    4. Verify initial commit readiness with `git status`.
  </action>
  <verify>test -f .gitignore && git status</verify>
  <acceptance_criteria>
    - Root .gitignore exists and ignores sensitive `.env` and build artifacts
    - `git status` shows unified workspace tracking
    - No nested `.git` conflict in expadvisor-frontend
  </acceptance_criteria>
  <done>Clean root Git repository tracking full ExpAdvisor codebase</done>
</task>

</tasks>
