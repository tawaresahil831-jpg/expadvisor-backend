---
phase: 03-frontend-build-asset-modernization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - expadvisor-frontend/package.json
  - expadvisor-frontend/tailwind.config.js
  - expadvisor-frontend/frontend/css/input.css
  - expadvisor-frontend/frontend/css/output.css
  - expadvisor-frontend/frontend/*.html
autonomous: true
requirements:
  - FE-01
user_setup: []
must_haves:
  truths:
    - "Tailwind CSS is compiled locally into a static bundle (frontend/css/output.css) rather than via runtime CDN"
    - "All frontend HTML pages link to output.css for deterministic styling and fast load times"
    - "npm run build:css produces a minified production CSS bundle"
  artifacts:
    - expadvisor-frontend/package.json
    - expadvisor-frontend/tailwind.config.js
    - expadvisor-frontend/frontend/css/input.css
    - expadvisor-frontend/frontend/css/output.css
  key_links:
    - "HTML pages link <link rel='stylesheet' href='css/output.css'> while keeping CDN as fallback or replacing it cleanly"
---

<objective>
Eliminate runtime browser-side Tailwind CSS compilation by setting up a build process with Tailwind CLI and generating a precompiled, optimized CSS bundle.

Purpose: Boost frontend performance, eliminate flash of unstyled content (FOUC), and adhere to production web best practices.
Output: Configured npm build tooling in `expadvisor-frontend/`, `tailwind.config.js`, and compiled `output.css`.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-frontend-build-asset-modernization/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize package.json and Tailwind CSS CLI</name>
  <files>expadvisor-frontend/package.json, expadvisor-frontend/tailwind.config.js, expadvisor-frontend/frontend/css/input.css</files>
  <read_first>expadvisor-frontend/vercel.json</read_first>
  <action>
    1. Initialize `package.json` in `expadvisor-frontend/` with scripts:
       - `"build:css": "tailwindcss -i ./frontend/css/input.css -o ./frontend/css/output.css --minify"`
       - `"watch:css": "tailwindcss -i ./frontend/css/input.css -o ./frontend/css/output.css --watch"`
    2. Install `tailwindcss` locally or via devDependencies.
    3. Create `expadvisor-frontend/tailwind.config.js` pointing to `./frontend/**/*.html` and `./frontend/**/*.js` with the existing brand color palette (`brand: { 50..700 }`, `surface`, `canvas`).
    4. Create `expadvisor-frontend/frontend/css/input.css` containing `@tailwind base; @tailwind components; @tailwind utilities;` plus custom scrollbar utilities.
  </action>
  <verify>test -f expadvisor-frontend/tailwind.config.js && test -f expadvisor-frontend/frontend/css/input.css</verify>
  <acceptance_criteria>
    - package.json exists with build:css script
    - tailwind.config.js matches design tokens
    - input.css configured with Tailwind directives
  </acceptance_criteria>
  <done>Tailwind build tooling established in expadvisor-frontend</done>
</task>

<task type="auto">
  <name>Task 2: Compile output.css and link into HTML pages</name>
  <files>expadvisor-frontend/frontend/css/output.css, expadvisor-frontend/frontend/*.html</files>
  <read_first>expadvisor-frontend/frontend/dashboard.html</read_first>
  <action>
    1. Run `npm --prefix expadvisor-frontend run build:css` (or npx tailwindcss) to generate `expadvisor-frontend/frontend/css/output.css`.
    2. In all HTML pages (`dashboard.html`, `dashboard_v2.html`, `login.html`, `register.html`, `my_profile.html`, `my_problems.html`, `solve.html`, `student_directory.html`, `forgot_pass.html`, `reset_password.html`, `check_mail.html`), include `<link rel="stylesheet" href="css/output.css">`.
    3. Remove or retain minimal fallback for the dynamic Tailwind CDN script.
  </action>
  <verify>test -f expadvisor-frontend/frontend/css/output.css && grep -l "output.css" expadvisor-frontend/frontend/dashboard.html</verify>
  <acceptance_criteria>
    - output.css exists and has size > 10KB
    - dashboard.html and login.html reference css/output.css
  </acceptance_criteria>
  <done>Static compiled CSS bundle integrated across frontend pages</done>
</task>

</tasks>
